service: real-user-api
frameworkVersion: '3'

provider:
  name: aws
  runtime: nodejs14.x



functions:
  api:
    handler: index.handler
    events:
      - httpApi:
          path: /
          method: get
